int fib(int n) {
    if(n == 0) {
        return 0;
    }
    if(n == 1) {
        return 1;
    }
    return (fib(n-1) + fib(n-2));
}

vector<int> generateFibonacciNumbers(int n) {
    // Write your code here.
    vector<int> ans;
    int i = 0;
    while(i < n) {
        ans.push_back(fib(i));
        i++;
    }
    
    return ans;
}
